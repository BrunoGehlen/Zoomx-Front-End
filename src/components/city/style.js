import styled from "styled-components";

export const CityStyles = styled.div`
    margin-bottom:12px;

    form {
        margin-bottom:12px;
        display:flex;
        flex-flow: column nowrap;

    }
`